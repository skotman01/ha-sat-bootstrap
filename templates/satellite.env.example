# Identity (single source of truth)
SAT_HOSTNAME=ha-satellite

# MQTT (agent)
MQTT_HOST=
MQTT_PORT=1883
MQTT_USER=
MQTT_PASS=
MQTT_BASE_TOPIC=ha/satellite


#Repo info
# Update architecture
Environment=REPO_DIR=/opt/ha-sat-bootstrap
Environment=UPDATE_UNIT_PREFIX=ha-sat-update@
